{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Run file",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "args": [],
            "console": "integratedTerminal",
            "cwd": "${fileDirname}"
        }
    ]
}